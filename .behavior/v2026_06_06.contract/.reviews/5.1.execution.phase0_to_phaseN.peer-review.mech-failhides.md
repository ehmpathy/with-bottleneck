✨ all clear
   ├─ logs: .log/bhrain/review/2026-06-07T03-35-53-484Z
   ├─ review: .behavior/v2026_06_06.contract/.reviews/5.1.execution.phase0_to_phaseN.peer-review.mech-failhides.md
   └─ summary
      ├─ 0 blockers
      └─ 2 nitpicks 🟠

---
# nitpick.1: Use .throw() syntax instead of new for error guards

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/pitofsuccess.errors/rule.require.failfast.md

**locations**:
- src/domain.operations/genBottleneck/assertLimitsValid.ts:14
- src/domain.operations/genBottleneck/assertLimitsValid.ts:21
- src/domain.operations/genBottleneck/assertLimitsValid.ts:28
- src/domain.operations/genBottleneck/assertLimitsValid.ts:34
- src/domain.operations/genBottleneck/assertLimitsValid.ts:41
- src/domain.operations/genBottleneck/assertLimitsValid.ts:47
- src/domain.operations/semaphore/genSemaphore.ts:53

The rule requires preferring BadRequestError.throw(...) and UnexpectedCodePathError.throw(...) for guard clauses and validations to enable clean 1-liners and consistent failfast patterns. The code uses throw new ErrorClass(...) instead, which works but does not follow the preferred syntax shown in the rule for early throws in validations and guards.

**snippet**:
```ts
if (limits.concurrency !== undefined && limits.concurrency <= 0)
    throw new BadRequestError('concurrency must be positive', {
      field: 'concurrency',
      value: limits.concurrency,
      hint: 'use a positive integer for concurrency limit, or omit for unlimited',
    });
```

---

# nitpick.2: Add blank lines after each guard clause per code structure

**rule**: .agent/repo=ehmpathy/role=mechanic/briefs/practices/code.prod/pitofsuccess.errors/rule.require.failfast.md

**locations**:
- src/domain.operations/genBottleneck/assertLimitsValid.ts:12
- src/domain.operations/genBottleneck/assertLimitsValid.ts:19
- src/domain.operations/genBottleneck/assertLimitsValid.ts:26
- src/domain.operations/genBottleneck/assertLimitsValid.ts:32

The rule specifies that each guard block must be treated as a code paragraph, preceded by a summary comment, and followed by a blank line before main logic or the next guard continues. In the validation function, consecutive if-throw guards for the same category (e.g., concurrency) lack blank lines between them, and comments cover groups rather than each individual guard.

**snippet**:
```ts
// validate concurrency: must be positive integer or Infinity
if (limits.concurrency !== undefined && limits.concurrency <= 0)
    throw new BadRequestError('concurrency must be positive', {
      field: 'concurrency',
      value: limits.concurrency,
      hint: 'use a positive integer for concurrency limit, or omit for unlimited',
    });

if (
    limits.concurrency !== undefined &&
    limits.concurrency !== Infinity &&
    !Number.isInteger(limits.concurrency)
  )
    throw new BadRequestError('concurrency must be an integer', {
      field: 'concurrency',
      value: limits.concurrency,
      hint: 'use a positive integer for concurrency limit, or Infinity for unlimited',
    });
```
