# internal research: production codepaths

## repo state

this is a greenfield repo. the only source file is:

```
src/index.ts
```

content:
```ts
// todo
```

**no production codepaths exist to research.**

---

## relevant patterns from ehmpathy ecosystem

the wish doc references patterns from related ehmpathy packages. these inform (but do not exist in) this repo.

### pattern 1: wrapper-fns HOF pattern

**source**: ehmpathy/wrapper-fns

**cite**: [1] vision.yield.md internal research section

> "extant `withBottleneck` uses npm `bottleneck` package"
> "follows `(logic, options) => wrappedLogic` pattern"
> "preserves function signature via generics"

**relevance**: [REUSE] — the HOF wrapper pattern is standard for ehmpathy. withBottleneck will follow this pattern.

---

### pattern 2: Wrapper type

**source**: ehmpathy/wrapper-fns

**cite**: [1] vision.yield.md internal research section

> "`Wrapper<TProcedure, TOptions>` type: `(logic, options) => logic`"
> "`Procedure` type for context-aware functions"

**relevance**: [EXTEND] — will use the `(input, context)` procedure signature but adapt to withBottleneck needs.

---

### pattern 3: factory function pattern

**source**: ehmpathy/simple-in-memory-queue

**cite**: [1] vision.yield.md internal research section

> "factory function pattern: `createQueue<T>({ order })`"
> "closure for state management"

**relevance**: [REUSE] — genBottleneck will follow factory function pattern with closure for semaphore/token state.

---

## summary

| pattern | source | action |
|---------|--------|--------|
| HOF wrapper `(fn, options) => fn` | wrapper-fns | [REUSE] |
| `(input, context)` signature | wrapper-fns | [EXTEND] |
| factory function with closure | simple-in-memory-queue | [REUSE] |

---

## citations

[1] `.behavior/v2026_06_06.contract/1.vision.yield.md` — internal research section, lines 218-235
