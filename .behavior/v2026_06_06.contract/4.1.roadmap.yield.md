# roadmap: with-bottleneck

execution roadmap for `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md`

---

## phase 0: project setup

**depends on**: —

**deliverable**: repo skeleton with package.json, tsconfig, test config

**before you start, read**:
- `.behavior/v2026_06_06.contract/0.wish.md` — core intent
- `.behavior/v2026_06_06.contract/1.vision.yield.md` — dependencies (iso-time)

**checklist**:
- [ ] initialize package.json with name `with-bottleneck`
- [ ] add dependency: `iso-time` (for IsoDuration type)
- [ ] add dev dependencies: typescript, jest, test-fns
- [ ] configure tsconfig.json for esm + cjs dual output
- [ ] configure jest with ts-jest
- [ ] create src/ directory structure per blueprint filediff tree

**acceptance criteria**:
- `npm install` completes without error
- `npm run build` compiles (empty src/index.ts)
- `npm test` runs (no tests yet, exit 0)

**verification**:
```bash
rhx git.repo.test --what types
```

---

## phase 1: domain.objects (types)

**depends on**: phase 0

**deliverable**: `src/domain.objects/Bottleneck.ts` with type definitions

**before you start, read**:
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — type signatures section

**checklist**:
- [ ] create `src/domain.objects/Bottleneck.ts`
- [ ] define `BottleneckSemaphore` interface
- [ ] define `BottleneckThrottle` interface
- [ ] define `Bottleneck` interface
- [ ] define `BottleneckSupplier` type

**acceptance criteria**:
- types compile without error
- types match blueprint signatures exactly

**verification**:
```bash
rhx git.repo.test --what types
```

---

## phase 2: genSemaphore + tests

**depends on**: phase 1

**deliverable**: `src/domain.operations/semaphore/genSemaphore.ts` + unit tests

**before you start, read**:
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — genSemaphore codepath, test scenarios
- `.behavior/v2026_06_06.contract/3.1.3.research.internal.product.code.test._.yield.md` — test patterns

**checklist**:
- [ ] create `src/domain.operations/semaphore/genSemaphore.ts`
- [ ] implement acquire() — blocks if at concurrency limit
- [ ] implement release() — frees slot, throws UnexpectedCodePathError if active is 0
- [ ] implement queued getter
- [ ] implement active getter
- [ ] create `src/domain.operations/semaphore/genSemaphore.test.ts`
- [ ] test [case1]: acquire/release cycle with concurrency 2
- [ ] test [case2]: release without acquire throws UnexpectedCodePathError
- [ ] add snapshot for release-without-acquire error

**acceptance criteria**:
- genSemaphore(2) limits concurrent operations to 2
- 3rd acquire blocks until release
- release without acquire throws UnexpectedCodePathError with details
- all unit tests pass
- error snapshot matches expected format

**verification**:
```bash
rhx git.repo.test --what unit --scope genSemaphore --mode apply
```

---

## phase 3: genThrottle + tests

**depends on**: phase 1

**deliverable**: `src/domain.operations/throttle/genThrottle.ts` + unit tests

**before you start, read**:
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — genThrottle codepath, test scenarios
- `.behavior/v2026_06_06.contract/3.1.1.research.external.product.flagged._.yield.md` — throttle algorithm choice
- `.behavior/v2026_06_06.contract/3.1.3.research.internal.product.code.test._.yield.md` — test patterns

**checklist**:
- [ ] create `src/domain.operations/throttle/genThrottle.ts`
- [ ] implement acquire() — blocks if tokens exhausted
- [ ] implement tokens getter
- [ ] implement refill logic (quantity per duration)
- [ ] create `src/domain.operations/throttle/genThrottle.test.ts`
- [ ] test [case1]: burst up to quantity
- [ ] test [case1]: 6th token blocks
- [ ] test [case1]: tokens refill after duration (time mocked)

**acceptance criteria**:
- genThrottle({ quantity: 5, duration: { seconds: 1 } }) allows 5 immediate acquires
- 6th acquire blocks until refill
- refill restores tokens to quantity after duration
- all unit tests pass with mocked time

**verification**:
```bash
rhx git.repo.test --what unit --scope genThrottle --mode apply
```

---

## phase 4: genBottleneck + tests

**depends on**: phase 2, phase 3

**deliverable**:
- `src/domain.operations/genBottleneck/assertLimitsValid.ts` + unit tests
- `src/domain.operations/genBottleneck.ts` + integration tests

**before you start, read**:
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — genBottleneck codepath, test scenarios
- `.behavior/v2026_06_06.contract/3.1.3.research.internal.product.code.test._.yield.md` — test patterns

**checklist**:
- [ ] create `src/domain.operations/genBottleneck/assertLimitsValid.ts`
- [ ] validate concurrency > 0 if present
- [ ] validate velocity.quantity > 0 if present
- [ ] throw ConstraintError with details on invalid
- [ ] create `src/domain.operations/genBottleneck/assertLimitsValid.test.ts`
- [ ] test [case1]: valid limits pass
- [ ] test [case2]: zero concurrency throws ConstraintError
- [ ] test [case2]: negative concurrency throws ConstraintError
- [ ] test [case2]: zero velocity.quantity throws ConstraintError
- [ ] test [case3]: undefined limits pass (defaults apply)
- [ ] add snapshots for error formats
- [ ] create `src/domain.operations/genBottleneck.ts`
- [ ] compose assertLimitsValid + genSemaphore + genThrottle
- [ ] implement schedule() to wrap semaphore + throttle
- [ ] create `src/domain.operations/genBottleneck.integration.test.ts`
- [ ] test [case1]: concurrency-only limits
- [ ] test [case2]: velocity-only limits
- [ ] test [case3]: combined limits
- [ ] test [case4]: empty limits (unlimited)

**acceptance criteria**:
- assertLimitsValid throws ConstraintError with field, value, hint
- genBottleneck composes semaphore and throttle correctly
- schedule() acquires semaphore first, then throttle (per blueprint order)
- all unit and integration tests pass
- error snapshots match expected format

**verification**:
```bash
rhx git.repo.test --what unit --scope assertLimitsValid --mode apply
rhx git.repo.test --what integration --scope genBottleneck --mode apply
```

---

## phase 5: withBottleneck + tests

**depends on**: phase 4

**deliverable**: `src/domain.operations/withBottleneck.ts` + integration tests

**before you start, read**:
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — withBottleneck codepath, test scenarios
- `.behavior/v2026_06_06.contract/3.1.3.research.internal.product.code.prod._.yield.md` — HOF pattern (withRetry)

**checklist**:
- [ ] create `src/domain.operations/withBottleneck.ts`
- [ ] accept static Bottleneck or BottleneckSupplier function
- [ ] derive bottleneck at call time via supplier
- [ ] delegate to bottleneck.schedule()
- [ ] preserve function signature
- [ ] create `src/domain.operations/withBottleneck.integration.test.ts`
- [ ] test [case1]: static bottleneck
- [ ] test [case2]: BottleneckSupplier function with different contexts
- [ ] test [case3]: error propagation and slot release on throw

**acceptance criteria**:
- withBottleneck(fn, { bottleneck }) returns function with same signature
- static bottleneck uses single instance
- supplier function called with (input, context) at call time
- errors propagate to caller unchanged
- slot released even on error
- all integration tests pass

**verification**:
```bash
rhx git.repo.test --what integration --scope withBottleneck --mode apply
```

---

## phase 6: public exports + acceptance tests

**depends on**: phase 5

**deliverable**:
- `src/index.ts` with public exports
- `src/index.acceptance.test.ts` with end-to-end scenarios

**before you start, read**:
- `.behavior/v2026_06_06.contract/2.1.criteria.blackbox.yield.md` — blackbox criteria
- `.behavior/v2026_06_06.contract/2.2.criteria.blackbox.matrix.yield.md` — criteria matrix
- `.behavior/v2026_06_06.contract/3.3.1.blueprint.product.yield.md` — acceptance test scenarios

**checklist**:
- [ ] create `src/index.ts`
- [ ] export Bottleneck type
- [ ] export genBottleneck function
- [ ] export withBottleneck function
- [ ] create `src/index.acceptance.test.ts`
- [ ] test [case1]: public exports available
- [ ] test [case2]: genBottleneck with valid limits
- [ ] test [case3]: genBottleneck with invalid limits
- [ ] test [case4]: withBottleneck wraps async function
- [ ] test [case5]: concurrency-controlled scenario (end-to-end)
- [ ] test [case6]: velocity-controlled scenario (end-to-end with time mock)
- [ ] add snapshots for Bottleneck structure
- [ ] add snapshots for error formats
- [ ] add snapshots for initial/final states

**acceptance criteria**:
- all public exports accessible via `import { ... } from 'with-bottleneck'`
- genBottleneck returns correct Bottleneck shape
- invalid limits throw ConstraintError with observability
- withBottleneck preserves signature, propagates errors
- concurrency scenario limits simultaneous operations correctly
- velocity scenario limits rate correctly with time mock
- all acceptance tests pass
- all snapshots match expected format

**verification**:
```bash
rhx git.repo.test --what types
rhx git.repo.test --what acceptance --mode apply
npm run build
```

---

## phase 7: final verification

**depends on**: phase 6

**deliverable**: all tests green, build passes, ready for release

**checklist**:
- [ ] run full test suite
- [ ] verify build output (esm + cjs)
- [ ] verify exports work correctly
- [ ] review all snapshots in PR

**verification**:
```bash
rhx git.repo.test --what all --mode apply
npm run build
npm pack
```

---

## summary

| phase | deliverable | test type |
|-------|-------------|-----------|
| 0 | project setup | types |
| 1 | domain.objects (types) | types |
| 2 | genSemaphore | unit |
| 3 | genThrottle | unit |
| 4 | assertLimitsValid + genBottleneck | unit + integration |
| 5 | withBottleneck | integration |
| 6 | index.ts exports | acceptance |
| 7 | final verification | all |
