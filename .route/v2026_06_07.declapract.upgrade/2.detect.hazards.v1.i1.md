# 2.detect.hazards — iteration 1

## summary

detected 1 blocker and 2 notes.

## hazards

### blocker: type error in jest.integration.env.ts

**what changed**: declapract added `{ cause: error }` to Error constructor at line 105

**why it breaks**: tsconfig.json has `target: "es2020"` which doesn't support the ErrorOptions argument (requires ES2022+)

**error**:
```
jest.integration.env.ts(105,7): error TS2554: Expected 0-1 arguments, but got 2.
```

**how to verify**: `rhx git.repo.test --what types`

**fix required**: update tsconfig target to es2022+ or remove the cause option

---

## notes (non-blockers)

### note: tsconfig module changed to node16

**what changed**: `module` and `moduleResolution` changed from `commonjs`/`node` to `node16`/`node16`

**why it may break**: different import resolution rules; may require `.js` extensions in imports for ESM

**how to verify**: `rhx git.repo.test --what unit --mode apply`

### note: biome now lints .agent/

**what changed**: biome.jsonc removed `!**/.agent` exclusion, now only excludes `!**/.agent/.cache`

**why it may break**: may flag lint errors in symlinked role files

**how to verify**: `rhx git.repo.test --what lint`

---

## next steps

1. fix the type error (stone 3.1 — repair test defects)
2. verify lint passes with .agent/ included
3. verify unit/integration tests pass with new module resolution
